# Telegram Manager - Python dependencies
# -----------------------------------------------------------------------------
# Tested on Python 3.9+

# Telegram MTProto client (multi-session, native 2FA support)
Telethon>=1.36.0,<2.0.0

# Proxy support used by Telethon for HTTP/SOCKS proxies
PySocks>=1.7.1
python-socks>=2.4.0

# Pretty terminal output (tables, colors, panels)
rich>=13.7.0

# Interactive prompts (menus, confirmations, password input)
questionary>=2.0.1

# Load API_ID/API_HASH from .env
python-dotenv>=1.0.0

# Cryptography backend for Telethon (faster than pure-python)
cryptg>=0.4.0

# Telegram Bot API framework (for remote bot management)
aiogram>=3.4.0,<4.0.0

# Supabase client (database storage)
supabase>=2.0.0
